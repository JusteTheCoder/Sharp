local Sharp = _G.Sharp

--[=[
	A library of useful math functions.
	@class MathUtil
]=]

return table.freeze({
	bezier = Sharp.package.MathUtil.Bezier,
	numbers = Sharp.package.MathUtil.Numbers,
	triangles = Sharp.package.MathUtil.Triangles,
})
